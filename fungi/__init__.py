"""Fungi — LAN multi-host Orchestrator network on the TriLayer agent harness.

The version lives solely in pyproject.toml (`fungi.update.local_version` reads it,
and the exe bundle ships a copy next to web/) — never mirror it in a constant
here: the one that used to sit below this docstring said 0.1.0 long after the
releases had moved on.
"""
